spring:
  datasource:
    url: jdbc:mysql://localhost:3306/web_pm?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    username:
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
 #       show_sql: true
 #       format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type : trace